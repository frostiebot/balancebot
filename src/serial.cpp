#include "serial.h"

// #include <esp_system.h>

// static TaskHandle_t serialCheckTaskHandle = 0;

void setupSerial() {
  // Serial.begin(SERIAL_BAUD_RATE);

  // serialCheckTaskHandle = 0;

  // xTaskCreatePinnedToCore(serialCheckTask, "serialCheckTask", 8192, NULL, 1, &serialCheckTaskHandle, 0);
}
